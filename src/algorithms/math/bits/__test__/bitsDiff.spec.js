const bitsDiff = require("../bitsDiff");
const countSetBits = require("../countSetBits");

describe("bitsDiff", () => {
    it("it should return the number of different bits between two numbers", () => { 
        expect(true).toBe(true);
        // Comment décomposer le test unitaire d'une fonction bitsDiff appelant une autre fonction countSetBits
        // expect(bitsDiff(0, 0)).toBe(countSetBits(0^0));
        // expect(bitsDiff(1, 1)).toBe(countSetBits(1^1));
        // expect(bitsDiff(124, 124)).toBe(countSetBits(124^124));
        // expect(bitsDiff(0, 1)).toBe(countSetBits(0^1));
        // expect(bitsDiff(1, 0)).toBe(countSetBits(1^0));
        // expect(bitsDiff(1, 2)).toBe(countSetBits(1^2));
        // expect(bitsDiff(1, 3)).toBe(countSetBits(1^3));
    });
});